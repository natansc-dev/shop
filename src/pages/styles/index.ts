import { createStitches } from '@stitches/react'

const {
  config,
  styled,
  css,
  globalCss,
  keyframes,
  theme,
  createTheme,
  getCssText,
} = createStitches({
  theme: {

  }
})